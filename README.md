# JSON-XML
As a human it would be easier to use CSV because of the interface that separates
the data into easily understood columns and rows. For computers JSON is easier to
read because it puts the data into objects so it is easier for a computer system
to read.
